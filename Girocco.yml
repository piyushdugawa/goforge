app:
  package: github.com/piyushdugawa/girocco
  version: 0.13.0
build:
  output: build/girocco
  optimisation: true

  env:
    GOOS: [linux, mac, windows]
    GOARCH: amd64

  flags:
    - -ldflags
    - "-s -w"
