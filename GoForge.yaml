app:
  package: github.com/piyushdugawa/goforge
  version: 0.13.0
build:
  output: build/goforge.exe
  optimisation: true

  env:
    GOOS: [windows, linux]
    GOARCH: amd64

  flags:
    - -ldflags
    - "-s -w"
