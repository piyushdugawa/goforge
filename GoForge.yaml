app:
  package: GoForge
  version: 0.11.1
build:
  output: build/goforge.exe
  optimisation: true

  env:
    GOOS: [windows, linux]
    GOARCH: amd64

  flags:
    - -ldflags
    - "-s -w"
