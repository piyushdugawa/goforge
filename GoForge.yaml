app:
  package: GoForge
  version: 0.10.2
build:
  output: build/goforge.exe
  optimisation: true

  env:
    GOOS: [windows, mac, linux]
    GOARCH: amd64

  flags:
    - -ldflags
    - "-s -w"
