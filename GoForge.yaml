app:
  package: GoForge
  version: 0.9.1
build:
  output: build/goforge.exe
  optimisation: true

  env:
    GOOS: [windows, mac, linux]
    GOARCH: amd64

  flags:
    - -ldflags
    - "-s -w"
